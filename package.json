{
  "name": "mega-vault-viewer-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "desktop:dev": "npm --workspace apps/desktop run tauri:dev",
    "desktop:build": "npm --workspace apps/desktop run tauri:build",
    "desktop:build:windows": "npm --workspace apps/desktop run tauri:build:windows",
    "build": "npm --workspace apps/desktop run build && cargo build --workspace",
    "test": "npm --workspace apps/desktop run test && cargo test --workspace",
    "smoke:minimal-starter": "node scripts/smoke-minimal-starter.mjs",
    "check": "npm test --if-present && npm run build --if-present && cargo fmt --all -- --check && cargo clippy --all-targets -- -D warnings && git diff --check"
  }
}
