{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for local vault viewing.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
